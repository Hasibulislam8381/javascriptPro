//reduce(accumulatorFunction , initial Value)
const numbers = [12, 3, 4, 1, 3];
const sum = numbers.reduce((previous, current) => previous + current, 0);
// here 0 is the initial value
console.log(sum);

// Find
